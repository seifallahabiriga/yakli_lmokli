# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20          # form data parsing

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0                   # async PostgreSQL driver (FastAPI)
psycopg2-binary==2.9.10           # sync PostgreSQL driver (Celery / Alembic)
alembic==1.14.0

# Redis + Celery
redis==5.2.1
celery[redis]==5.4.0
flower==2.0.1                     # Celery monitoring UI

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Config
pydantic==2.10.3
pydantic-settings==2.6.1

# ML — local models
numpy==2.1.3
scikit-learn==1.5.2
sentence-transformers==3.3.1
torch==2.5.1                      # sentence-transformers backend (CPU)
faiss-cpu==1.9.0                  # ANN index — use faiss-gpu if GPU available
spacy==3.8.3
# After install run: python -m spacy download en_core_web_sm

# ML — cloud LLM providers
google-generativeai==0.8.3        # Gemini
groq==0.12.0                      # Groq

# Scraping
httpx==0.28.1                     # async HTTP client (FastAPI side)
requests==2.32.3                  # sync HTTP client (scraper side)
beautifulsoup4==4.12.3
lxml==5.3.0                       # BS4 parser backend
playwright==1.49.0
# After install run: playwright install chromium

# NLP utilities
nltk==3.9.1

# Monitoring
prometheus-client==0.21.1
loguru==0.7.3

# Mesa — multi-agent framework
mesa==3.1.0

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0                   # retry logic for scrapers
email-validator==2.2.0            # Pydantic EmailStr validation

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1                     # also used as AsyncClient in tests
factory-boy==3.3.1                # test fixtures
faker==33.1.0